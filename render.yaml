services:
  - type: web
    name: arabmonopoly-api
    runtime: docker
    rootDir: backend
    plan: free
    healthCheckPath: /api/dashboard/summary
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 8080
